<div class="col-md-8 col-xs-10">
	<h1>Wikis</h1>
	<%= render partial: 'wikis/list', locals: {wikis: @wikis} %>
</div>
<div class="col-md-4 col-xs-12">
	<% if current_user %>
		<%= link_to "Create Wiki", new_wiki_path, class: 'btn btn-lg btn-success' %>
	<% end %>
</div>